# Local .env is optional now.
# Use Doppler as the real source of secrets:
#   npm run doppler:dev
#   npm run doppler:typecheck
#   npm run doppler:build

# Uncomment only if you need a temporary local fallback.
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
