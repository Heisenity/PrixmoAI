{
    "name": "server",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "doppler run -- tsc -p tsconfig.json",
        "doppler:build": "doppler run -- tsc -p tsconfig.json",
        "doppler:dev": "doppler run -- nodemon --exec ts-node src/index.ts",
        "doppler:evaluate:brand-memory": "doppler run -- ts-node src/scripts/evaluateBrandMemory.ts",
        "doppler:archive:historical-data": "doppler run -- ts-node src/scripts/archiveHistoricalData.ts",
        "doppler:ensure:super-admin": "doppler run -- ts-node src/scripts/ensureSuperAdmin.ts",
        "doppler:start": "doppler run -- node dist/src/index.js",
        "doppler:gemini:test": "doppler run -- ts-node src/scripts/testGemini.ts",
        "doppler:typecheck": "doppler run -- tsc --noEmit -p tsconfig.json",
        "archive:historical-data": "doppler run -- ts-node src/scripts/archiveHistoricalData.ts",
        "start": "doppler run -- node dist/src/index.js",
        "typecheck": "doppler run -- tsc --noEmit -p tsconfig.json",
        "evaluate:brand-memory": "doppler run -- ts-node src/scripts/evaluateBrandMemory.ts",
        "ensure:super-admin": "doppler run -- ts-node src/scripts/ensureSuperAdmin.ts",
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "doppler run -- nodemon --exec ts-node src/index.ts"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "commonjs",
    "dependencies": {
        "@aws-sdk/client-s3": "^3.1032.0",
        "@huggingface/inference": "^4.13.15",
        "@supabase/supabase-js": "^2.99.2",
        "bullmq": "^5.74.1",
        "cors": "^2.8.6",
        "dotenv": "^17.3.1",
        "express": "^5.2.1",
        "helmet": "^8.1.0",
        "ioredis": "^5.10.1",
        "zod": "^4.3.6"
    },
    "devDependencies": {
        "@types/cors": "^2.8.19",
        "@types/express": "^5.0.6",
        "nodemon": "^3.1.14",
        "ts-node": "^10.9.2",
        "typescript": "^5.9.3"
    }
}
